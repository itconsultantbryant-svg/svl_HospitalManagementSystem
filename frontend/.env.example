# Vercel: set this in Project → Settings → Environment Variables (Production + Preview).
# Value = Neon Function invocation URL (no trailing slash), e.g.
#   https://br-xxxx-api.compute.c-7.us-east-2.aws.neon.tech
VITE_API_URL=http://localhost:8787
