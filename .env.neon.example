# Local secrets for `neon deploy` (JWT / CORS). Never commit this file.
# Copy from .env.neon.example and fill in values.

JWT_SECRET=
CORS_ORIGIN=https://your-app.vercel.app
FRONTEND_URL=https://your-app.vercel.app
JWT_EXPIRES_IN=8h
LRD_PER_USD=193.5
